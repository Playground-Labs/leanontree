backend:
  name: git-gateway
  branch: migration/jekyllTovueJs # Branch to update (optional; defaults to master)

# This line should *not* be indented
publish_mode: editorial_workflow
# This line should *not* be indented
media_folder: "assets/images/upload" # Media files will be stored in the repo under images/uploads